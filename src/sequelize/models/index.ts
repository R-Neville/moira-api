import initUserModel from "./user.model";
import initProjectModel from "./project.model";
import initTaskModel from "./task.model";

const models = [initUserModel, initProjectModel, initTaskModel];

export default models;
